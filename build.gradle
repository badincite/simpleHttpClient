// Root build.gradle – do NOT apply Niagara scripts here.
// Just tell Gradle to load each subproject’s own *.gradle file.
subprojects {
    apply from: "${rootDir}/${project.name}/${project.name}.gradle"
}